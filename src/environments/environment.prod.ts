/**
 * Environment configuration for production
 * apiUrl: Cambiar por URL real del backend en producción
 */
export const environment = {
  production: true,
  apiUrl: 'https://api.tirito.app/api'
};
